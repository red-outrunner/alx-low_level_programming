#include <stdio.h>

/**
 * main - a program that uses the printf function to print a line
 * Return: 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
		return (0);
}
